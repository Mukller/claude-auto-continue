feat: авто-проверка обновлений + PerMonitorV2 DPI; починен потерянный бамп версии

## Update checker
Один тихий запрос GitHub Releases на запуск (GUI — фоновый поток в лог, headless — строка в stdout). Репортится строго более новый тег (semver-сравнение), офлайн безопасен, без трекинга.

## PerMonitorV2 DPI
`SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2)` с фолбэком на system-DPI для Windows < 1703: физические пиксели на каждом мониторе — клики не плывут на смешанных конфигурациях.

## Fixed
Сквош #36 потерял коммит с `__version__ = '3.14.0'` (на main оставалось 3.13.1 при докстринге 3.14.0). Восстановлено; релиз — v3.15.0.

Тесты: версия обновлена на 3.15.0. Всего 39/39.
